# Iteration 4 Operator Decision Addendum

Status: **PREPARATION AUTHORIZATIONS RECORDED - DECISION GATES REMAIN OPEN**
Recorded: `2026-07-28T08:48:19Z`
Project: `github.com/chronodeai/agentmemory`
Candidate source: `0e9af82dcfdf07dd1f521c4621823f31a9b2eaba`
Controlling frozen evidence revision: Revision 21

## Decision boundary

This append-only addendum normalizes the six annotated selections followed by
the operator instruction `GO`. It does not rewrite or retroactively enter the
Revision 21 manifest. Any later decision that relies on this addendum requires
a successor manifest and matching passed verification receipt.

`GO` authorizes execution only of choices that are both selected and complete
in the annotations. It is not a wildcard approval and does not fill an
unselected alternative, unnamed Railway scope, blank human role, missing
artifact digest, or exact phrase required by another gate.

## Annotation dispositions

### Annotation 1

Selected:

> SELECT HEALTH OPTION B.
> AUTHORIZE MINIMAL EXTERNAL-MODEL FRESH-HOST PROBES.

Disposition: **RECORDED PREVIOUSLY AND SATISFIED FOR THE EXACT TEMPORARY
CONTAINMENT SUBJECT**.

The completed Option B gate and one Codex plus one Claude fresh-host
provider-native compaction qualification remain the controlling evidence.
This duplicate selection does not authorize another model call or widen the
probe payload.

### Annotation 2

The selection is byte-identical to Annotation 1.

Disposition: **DEDUPLICATED - NO SECOND AUTHORIZATION OR EXECUTION**.

### Annotation 3

The selection is byte-identical to Annotation 1.

Disposition: **DEDUPLICATED - NO THIRD AUTHORIZATION OR EXECUTION**.

### Annotation 4

Selected:

> ACCEPT temporary containment.
> DEFER upstream supervisor handoff until containment probes pass.

Disposition:

- temporary containment remains **ACCEPTED AND APPLIED**;
- the predecessor upstream-supervisor handoff remains **DEFERRED**;
- the completed containment probes do not revive that invalidated handoff,
  because the installed runtime is fork-derived rather than an identified
  official-upstream rollback subject; and
- any successor handoff still requires a new exact artifact-, plist-,
  authentication-, rollback-, and interruption-bound decision.

### Annotation 5

Disposition by field:

| Field | Normalized disposition | Effect |
|---|---|---|
| Railway account/project scopes | **OPEN** | `[account and project]` is not a named stable scope, and `YES / NO / UNKNOWN` contains no selected answer. No scope is dispositioned. |
| Architecture evidence preparation | **AUTHORIZED** | Prepare C1, C2, and C3 fixtures, case-card inputs, veto evidence definitions, and scoring inputs only. Do not score through an open veto or select a configuration. |
| R-13 Stage-A specification | **INTENT TO ACCEPT RECORDED; FORMAL ACCEPTANCE OPEN** | The short form lacks the required named Test Architect, Configuration Manager, Security Architect, Release Owner, CI Owner, canonical manifest/hash, matching passed receipt, governed artifact hashes, and finding dispositions. It cannot change the candidate status. |
| PoC card input completion | **AUTHORIZED** | Complete technical specification inputs for all 17 P1 cards. Do not create a B1 mechanics authorization, B2 execution admission, executable evidence claim, or risk disposition. |
| Temporary runtime containment | **ACCEPTED** | Preserve the qualified reversible profile and advisory-only explicit recall posture. |
| Upstream supervisor handoff | **DEFERRED** | No service handoff, restart, plist bootstrap, or live-worker interruption. |
| AIWG authorization normalization | **DEFERRED** | Leave the legacy project authorization source and Doctor warning unchanged. |
| Human role lines | **OPEN** | Blank lines do not assign or impersonate a human decision owner. |

### Annotation 6

Every decision line contains unselected alternatives and every named-role line
is blank.

Disposition: **NO ADDITIONAL DECISION RECORDED**.

This template does not override Annotation 5 and does not select Railway
history, architecture return/authorization, R-13 acceptance/rejection, card
completion, containment, supervisor handoff, AIWG normalization, or an
independent verifier.

## Work authorized now

1. Produce a bounded C1/C2/C3 evidence-preparation gap analysis.
2. Complete technical, non-human inputs in the 17 P1 PoC specification cards.
3. Prepare exact owner/concurrence and artifact-digest fields for later human
   completion without assigning people or recording acceptance.
4. Reconcile resulting documentary links and prepare a successor deterministic
   manifest and verification receipt.

## Work not authorized

This addendum does not authorize:

- access to Railway credentials, projects, logs, volumes, backups, or secrets;
- another external-model probe or source-review call;
- R-13 Stage-A acceptance without the required complete decision record;
- PoC B1 mechanics preparation, B2 execution admission, `build-poc`, or any
  qualifying execution;
- product source, governed product test, CI, schema, package, runtime, viewer,
  plugin, deployment, or supervisor changes;
- official rollback Stage A preparation, because the exact
  `AUTHORIZE OFFICIAL ROLLBACK ARTIFACT PREPARATION ...` phrase is absent;
- administrator authentication, root work, live Agentmemory interruption,
  secret access, or production-state access;
- architecture selection, ADR acceptance, SAD baseline, risk acceptance or
  retirement, ABM passage, Construction, canary, release, distribution, or
  Memetics rollout.

## Required next decisions

The following remain independently open:

1. Enumerated Railway account/project scopes and one `YES`, `NO`, or `UNKNOWN`
   answer for each.
2. The complete R-13 Stage-A human decision record and named concurrences.
3. The exact official rollback Stage A authorization phrase.
4. One exact B1 mechanics authorization and later B2 execution admission for
   each PoC selected for execution.
5. Human architecture selection and ADR/SAD dispositions after veto evidence.
6. Independent ABM rerun and separate Construction authorization.

