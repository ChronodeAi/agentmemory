# Bounded PoC Plan

Status: Specification candidate - no qualifying PoC execution admitted
Date: 2026-07-26
Iteration: Elaboration iteration 4
Product-source baseline: `0e9af82dcfdf07dd1f521c4621823f31a9b2eaba`
Scope: ABM risk evidence only

## Guardrails

- Use isolated temporary homes, synthetic repositories, synthetic secrets,
  disposable iii-engine state, and recording provider sinks.
- Do not use production memory, production credentials, live user content,
  deployed provider directories, or the Memetics canary.
- Pin each run to the source SHA and fixture-manifest SHA.
- Stop on the first cross-project disclosure, raw secret occurrence,
  unauthenticated protected response, prohibited egress, mixed migration
  generation, unbounded resource growth, or corrupted rollback.
- A passing PoC creates evidence for later owner and gate review only. It does
  not change a risk status, accept an ADR, baseline architecture, or authorize
  Construction, distribution, rollout, or production use.

The executable case-card authority is `.aiwg/risks/poc-cards/README.md`.
No risk is currently ready for qualifying execution. The disposable R-13
harness under `.aiwg/working/` has run preparatory self-tests, but those runs
predate card admission and demonstrate mechanics only.

## Common Receipt

Every run must capture:

- risk ID, card version, source SHA, fixture SHA, artifact SHA;
- argv as a structured array, start/end time, duration, exit code, and signal;
- OS, architecture, CPU, RAM, Node, npm, Vitest, iii-engine, and Agentmemory
  versions;
- worker count, ports, non-secret feature flags, process-tree peak RSS, and
  disk/log growth;
- test IDs, tracked-file denominator, skipped-test list, and raw result hashes;
- input/output counts, project IDs, provider attempts, and typed failure states;
- synthetic-secret scan results without recording any real credential value;
- execution operator identity and immutable raw-receipt signer.

Before any card starts, `G-ICM-01` must enumerate the relevant REST, MCP, hook,
viewer, provider, authentication, project-scope, and failure-state surfaces.
If the matrix cannot identify the complete surface denominator, the card is
blocked rather than partially passed.

After a run receipt is sealed, the accountable owner and every named independent
reviewer must record a separate disposition against that immutable receipt.
Those post-run dispositions are review evidence and do not alter risk status
without a later authorized gate cycle.

## R-01 - Canonical Identity Isolation

- Owner: Software Architect.
- Reviewers: Security Architect, Test Architect, Configuration Manager.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-13 harness.
- Hypothesis: owner-declared equivalent SCP/SSH/HTTPS transport forms and
  worktrees resolve to one canonical repository identity, while distinct
  hosts, non-default ports, case-sensitive paths, colliding basenames, and
  ownership-unproven aliases remain distinct or fail closed.
- Pass evidence: every fixture resolves to its frozen expected equivalence
  class; explicit IDs and aliases pass ownership/collision checks; local
  repositories retain a stable repository UUID across moves; and zero
  out-of-project IDs or content appear across any scoped interface in
  `G-ICM-01`.
- Fail evidence: any collision, cross-project result, unscoped implicit read,
  or unresolved alias.
- Containment/backtrack: reject ambiguous identities, preserve the pre-run
  alias manifest, quarantine fixture records, and discard the isolated state.

## R-02 - Secret Flow

- Owner: Security Architect.
- Reviewers: Privacy Owner, Test Architect, Operations Owner.
- Timebox: 3 days.
- Dependencies: `G-ICM-01`, R-13 harness, complete state/log/export inventory.
- Hypothesis: labelled literal, encoded, structured, multiline, malformed-tag,
  error-path, image-metadata, and synthetic-PII fixtures are dropped or
  redacted before any persistence or disclosure boundary.
- Pass evidence: zero raw canary occurrences in KV/SQLite, BM25/vector state,
  logs, audits, viewer/API output, exports, snapshots, Git history, backups,
  provider payloads, delivery queues, error objects, failure remnants, and
  post-rollback state.
  The evidence set separately proves pre-transport redaction for hook
  input/output before local or remote delivery receives the payload,
  centralized sanitization for stderr/exception paths, and Railway
  source/documentation parity. Named cases:
  `R02-SECRET-CORPUS-ENDTOEND`, `R02-PRETRANSPORT-REDACTION`,
  `R02-LOG-SINK-TAINT`, and `R02-RAILWAY-HISTORY-CONTAINMENT`.
- Fail evidence: any unexpected raw canary occurrence or a redaction event
  without a traceable disposition.
- Containment/backtrack: suspend the affected ingestion path, restrict access
  to fixture artifacts, preserve incident evidence, destroy isolated state,
  and leave real credential response to separately authorized operations.

## R-03 - Authority Eligibility

- Owner: Product Owner.
- Reviewers: Test Architect, Software Architect, Security Architect.
- Timebox: 3 days.
- Dependencies: R-04, R-06, R-17, typed evidence schema.
- Hypothesis: eligibility is evaluated before relevance, and expired,
  contradicted, superseded, deleted, unaccepted, recalled-only, and
  provenance-less items cannot enter a gate-critical packet.
- Pass evidence: zero ineligible items in a frozen human-labelled corpus, an
  explicit exclusion reason for every omitted candidate, no duplicate source
  ID, and packet size at or below 2,000 tokens.
- Fail evidence: any stale/ineligible inclusion, untraceable exclusion, or
  relevance score overriding an eligibility failure.
- Containment/backtrack: disable gate-critical automatic injection, invalidate
  affected fixture packets, and revert to explicit advisory recall.

## R-04 - Delivery Acknowledgement

- Owner: Software Architect.
- Reviewers: Provider Integration Owner, Test Architect, Product Owner.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-13 harness, packet/receipt identifier contract.
- Hypothesis: only a valid matching provider acknowledgement changes delivery
  state; generation and failed dispatch leave all sources retryable.
- Pass evidence: failures before, during, and after dispatch do not suppress
  sources; one valid acknowledgement suppresses once; duplicate, invalid, and
  expired acknowledgements are idempotent or rejected; timeout retry succeeds.
  Local stdout or stream-buffer acceptance proves at most
  `DISPATCHED_UNVERIFIED`; it never proves provider-native acknowledgement,
  consumption, or suppression authority. Named cases:
  `R04-PROVIDER-NATIVE-DELIVERY`,
  `R04-CONTEXT-DELIVERY-STATE-MACHINE`, and
  `R04-PRECOMPACT-PROVIDER-ACK`.
- Fail evidence: generation alone marks a source delivered, a failed dispatch
  suppresses retry, or duplicate acknowledgement consumes twice.
- Containment/backtrack: disable automatic packet injection, clear only
  fixture delivery markers, and replay from the immutable source ledger.

## R-05 - Promotion Evidence Lineage

- Owner: Software Architect.
- Reviewers: Product Owner, Test Architect, Security Architect.
- Timebox: 2 days.
- Dependencies: R-03, R-04, R-06, R-17.
- Hypothesis: recalled, summarized, paraphrased, or transitively self-derived
  evidence cannot qualify a promotion without fresh independent typed proof.
- Pass evidence: all recall-only and cyclic evidence graphs reject; a valid
  test/runtime/commit receipt creates one traceable acyclic candidate; an
  architecture candidate requires a reference to an accepted external ADR
  without duplicating the decision. Semantic similarity cannot collapse
  independently sourced corroboration, and recalled or lesson-derived content
  cannot count as fresh evidence for itself or its descendants.
- Fail evidence: any self-certifying cycle, regex-only verification, or
  unresolved source ID reaches promotable state.
- Containment/backtrack: keep automatic promotion and native-memory
  synchronization disabled and quarantine all fixture candidates.

## R-06 - Dirty-to-Commit Provenance

- Owner: Git/Runtime Owner.
- Reviewers: Configuration Manager, Test Architect, Software Architect.
- Timebox: 3 days.
- Dependencies: R-01, R-13.
- Hypothesis: every qualifying uncommitted event is attributable to canonical
  project, worktree, base revision, path, content/diff digest, observation
  time, and later rename/delete/commit transition.
- Pass evidence: 100% fixture lineage is correct, edit/delete/supersession is
  idempotent, and at least 95% of the accepted representative eligible
  denominator links to a commit without attributing dirty evidence as clean.
  Shell/script writes are found by lifecycle-boundary reconciliation, commit
  receipts reference exact dirty event IDs, and the server derives and
  verifies the commit object, worktree ancestry, path, blob/diff, and parent
  relationships instead of trusting a supplied SHA. Commit creation and
  session linkage expose one atomic state or a recoverable journal, and the
  predeclared denominator and exclusions are emitted before calculating
  linkage. Named cases:
  `R06-DIRTY-COMMIT-LINEAGE`, `R06-SHELL-BOUNDARY`, and
  `R06-LINKAGE-DENOMINATOR`.
- Fail evidence: missing or fabricated lineage, denominator inflation,
  cross-worktree attribution, or stale evidence surviving supersession.
- Containment/backtrack: mark uncertain evidence ineligible, disable
  commit-derived promotion, and discard the fixture namespace.

## R-07 - Bounded Capture and Observability

- Owner: Performance Test Owner.
- Reviewers: Service Owner, Test Architect, Operations Owner.
- Timebox: 2 days.
- Dependencies: R-13 harness, declared host/concurrency profile.
- Hypothesis: four-agent capture and context traffic remains bounded while the
  generated runtime keeps sampling at or below 0.1 and console logging off.
- Pass evidence: p95 below 2 seconds by hook class, p99 below 5 seconds, no
  forced hook timeout, maximum capture queue depth at or below 256 events, zero
  dropped events, zero recursive trace IDs, peak process-tree RSS at or below
  the lower of 4 GiB or 50% of host RAM, disk/log growth at or below 512 MiB
  over 30 minutes, and candidate config hash matching the receipt.
  The receipt distinguishes active concurrency, queued, rejected, dropped,
  failed, retried, and delivered events; an active-operation counter cannot be
  labelled queue depth. Host-visible success is emitted only after a durable
  acceptance receipt, while every drop or timeout is explicit and bounded.
  Named cases: `R07-FOUR-AGENT-30M-LOAD`,
  `R07-HOOK-CLASS-LATENCY`, `R07-CAPACITY-SEMANTICS`,
  `R07-RAILWAY-CONFIG-PARITY`, and `R07-TRACE-RECURSION`.
- Fail evidence: threshold breach, absent queue/drop telemetry, event loss,
  deadlock, recursive log amplification, or resource growth beyond either fixed
  ceiling.
- Containment/backtrack: stop load immediately, cap concurrency, disable
  nonessential capture, restore the known bounded telemetry profile, and
  preserve only redacted receipts.

## R-08 - Readiness Truthfulness

- Owner: Service Owner.
- Reviewers: Operations Owner, Test Architect, Software Architect.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-07.
- Hypothesis: required KV or worker failure and sustained memory pressure can
  never produce a healthy readiness result.
- Pass evidence: required dependency failure becomes non-healthy within one
  30-second collection; recovery requires three consecutive successful
  probes; pressure transitions are timestamped and do not flap.
- Fail evidence: healthy status while authority is unavailable, missing
  component diagnostics, or recovery before the accepted success window.
- Containment/backtrack: remove the fixture service from readiness, stop
  gate-critical consumers, and restore prior threshold configuration.

## R-09 - Slot, Viewer, and Build Compatibility

- Owner: UI/API Owner.
- Reviewers: Service Owner, Test Architect, Configuration Manager.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-01, R-08, R-13, accepted
  health/fetch/compatibility enums, reproducible build identity, and complete
  scope/slot/browser fixtures.
- Current finding: the candidate and installed fork-derived,
  upstream-labelled viewer return `null` for a valid HTTP 503 critical-health
  body and can render `Unknown`.
  ChronodeAi `origin/main` commit
  `a8e7d19a814a24a21818afc715f3301b3eaeee80` contains a focused, unported
  repair that preserves the shared helper's default non-2xx failure contract.
  It is design input, not accepted implementation or retirement evidence.
- Hypothesis: scoped slots and viewer data remain correct only for compatible
  backend/viewer/API-contract builds; every valid health body, including a
  non-2xx body, retains its exact service state; and every unavailable,
  unauthorized, malformed, stale, incompatible, or disabled component is
  shown as a distinct typed state rather than healthy, compatible, current, or
  `Unknown`.
- Pass evidence: the frozen HTTP/body/browser matrix renders exact
  health/fetch/compatibility states; exact scoped slot CRUD/list behavior;
  zero opaque HTTP 500 responses; matching build hashes for supported pairs;
  explicit incompatibility or not-evaluated state for mismatched/missing
  pairs; project/global isolation; and explicit scope, denominator, snapshot,
  time, and authorization for counters and actions. A health-specific
  non-2xx-body rule must preserve failure behavior for every unrelated shared
  helper caller. Named cases: `R09-CRITICAL-503-BODY`,
  `R09-FETCH-COMPATIBILITY-MATRIX`, `R09-SHARED-HELPER-NONREGRESSION`,
  `R09-SLOT-SCOPE-TRUTHFULNESS`, and `R09-VIEWER-SNAPSHOT-AUTHORITY`.
- Fail evidence: discarded valid health body, cross-scope slot or memory data,
  hidden component failure, stale viewer state presented as current, unknown
  or false-compatible identity, false health, or unrelated-helper regression.
- Containment/backtrack: stop the disposable viewer at first false-health,
  cross-project, unauthorized-action, or shared-helper regression; retain
  redacted authenticated API/browser evidence; and restore the exact fixture
  backend/viewer pair without changing the installed runtime.
- Admission state: card version 1 is `SPECIFICATION-CANDIDATE`; execution is
  not admitted.

## R-10 - Codebase Memory Canonical Alias

- Owner: Codebase Memory Maintainer.
- Reviewers: Software Architect, Configuration Manager, Test Architect.
- Timebox: 2 days.
- Dependencies: R-01, R-16, external maintainer availability.
- Hypothesis: one canonical project identity serves all consumers while a
  temporary alias produces exactly equivalent counts and a frozen 20-query
  manifest: five symbol searches, five caller/callee traces, five exact source
  snippets, and five ADR/decision lookups. The exact query inputs are hashed in
  the fixture manifest before execution.
- Pass evidence: sandbox-copy configuration, node/edge/file counts, query
  hashes, normalized result hashes and counts for all 20 queries,
  source/decision roots, path filters, and rollback hashes agree; no consumer
  writes to a duplicate key.
- Fail evidence: divergent symbols/results, stale consumer, lossy alias,
  unbounded generated artifacts, or non-deterministic rollback.
- Containment/backtrack: use sandbox copies only, preserve both live indexes,
  prohibit duplicate retirement, and stop at first divergence.

## R-11 - Connector Repair and Rollback

- Owner: Connector Owner.
- Reviewers: Provider Integration Owners, Configuration Manager, Test Architect.
- Timebox: 1 day.
- Dependencies: `G-ICM-01`, R-13, disposable provider homes.
- Hypothesis: Codex and Claude repair changes only entries proven
  Agentmemory-owned by an exact ownership marker rather than command/path
  resemblance, merges unrelated hooks, remains byte-stable on rerun, and
  restores exactly without disclosing sensitive configuration in backups.
- Pass evidence: unrelated configuration hash is unchanged, second repair has
  no diff, symlink/owner/mode and malformed-input attacks fail closed,
  interruption is recoverable, backups contain no raw synthetic secret, and
  rollback reproduces the pre-image hash.
- Fail evidence: unrelated mutation, duplicate hook, force-only repair,
  partial write, or non-idempotent rerun.
- Containment/backtrack: operate on copied homes only and restore the verified
  pre-image without touching real provider configuration.

## R-12 - Offline Release Admission

- Owner: AIWG Maintainer.
- Reviewers: Release Owner, Security Architect, Configuration Manager.
- Timebox: 1 day.
- Dependencies: all evidence required by the frozen admission criteria below.
- Frozen admission criteria: complete R-01 through R-23 evidence index; all P0
  and all 17 P1 risks later retired or mitigated; at least 17 of 23 portfolio
  risks later retired or mitigated; zero unresolved secret, authentication, or egress
  failures; artifact digest, SBOM, and provenance; explicit Release Owner
  disposition; separate distribution authorization; and every other accepted
  gate criterion.
- Hypothesis: a no-publish rehearsal fails closed whenever a required receipt,
  artifact digest, owner disposition, or separate distribution authorization
  is absent.
- Pass evidence: every frozen admission criterion is represented in a complete
  local manifest, negative admission tests reject each missing criterion, and
  there is zero network publication or external repository write.
- Fail evidence: partial/draft evidence admits a package, a publish-capable
  path runs, or release and rollout authorization are conflated.
- Containment/backtrack: use a local archive only, provide no publication
  credentials, and delete only disposable package output.

## R-13 - Deterministic Complete Evidence Harness

- Owner: Test Infrastructure Owner.
- Reviewers: Test Architect, CI Owner, Service Owner.
- Timebox: 3 days.
- Dependencies: `G-ICM-01`, R-13 card version 3, human Stage-A acceptance of
  the MTP/profile/card/threshold specification, later B1 authorization of one
  disposable mechanics scope, and later B2 admission of the exact
  source/disposable-bundle/profile/fixture/policy set.
- Hypothesis: the single mandatory local profile
  `R13-LOCAL-MAC26.5.1-25F80-ARM64-N24.16.0-NPM11.13.0-V1` executes all 148
  tracked `*.test.ts` files plus the accepted assertion and
  bearer/project-capability
  authentication manifests with exactly one observed Vitest worker, an
  explicit child-environment allowlist, peak process-tree RSS at or below the
  lower of 4 GiB or 50% of host RAM, and wall time at or below 30 minutes per
  run. The historical 1,629 count remains provisional until Stage A accepts an
  assertion-identity manifest.
- Pass evidence: five consecutive exit-zero runs for the accepted mandatory
  local profile, no
  signal, all 148 tracked `*.test.ts` files accounted for, exactly one Vitest
  worker, explicit integration policy, missing mandatory auth setup fails
  instead of skips, and both fixed resource ceilings hold for every run.
  A separate validator reconstructs or opens the immutable source bundle,
  validates a Configuration Manager-controlled iii digest anchor, verifies
  operator and independent signer identities, and rejects every waiver in
  qualification-required mode. A successful `npm test` process or adjacent
  runner-generated checksum is insufficient by itself. The production-format
  candidate is an in-toto Statement v1 inside DSSE with a versioned R-13
  predicate and identity-bound verification policy; the PoC may use a
  synthetic key to prove mechanics but cannot claim accepted signer authority.
- Current feasibility: current CI supplies only the bearer fixture and
  therefore fails mandatory preflight before Vitest; current profile,
  environment, source, denominator, worker, signer, cohort, replay, and custody
  behavior does not conform to card v3. See
  `.aiwg/testing/r13-implementation-conformance-matrix.md`.
- Fail evidence: exit 137, flake, omitted file, hidden skip, unbounded worker
  count, missing machine-readable receipt, or drift from the accepted local
  profile. Node 22, Ubuntu, and GitHub CI remain
  `DEFERRED-PORTABILITY` / `DEFERRED-LOCAL-TARGET`; they require a later exact
  profile and separate admission and cannot substitute for this denominator.
- Containment/backtrack: isolated temporary KV, bounded workers, hard resource
  ceiling, terminated process tree on breach, and clean environment restore.

## R-14 - Required Authentication

- Owner: Authentication Service Owner.
- Reviewers: Security Architect, Test Architect, Operations Owner.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-13, isolated externally reachable fixture.
- Hypothesis: a missing or unreadable authentication secret prevents protected
  service admission; missing, malformed, wrong, expired, wrong-project, or
  wrong-operation credentials and capabilities never succeed or downgrade to a
  less authoritative path.
- Pass evidence: missing/unreadable secret refuses startup or returns typed
  unavailable on every protected endpoint; missing/wrong credentials return
  401; only enumerated health endpoints are public; tests cannot skip.
  The externally reachable black-box matrix covers all 134 protected REST
  routes and every MCP transport, with project/resource/operation capability
  boundaries and the R-18 downgrade matrix. A public deployment profile cannot
  generate an undisclosed first-boot credential and continue binding. Named
  cases:
  `R14-AUTH-DENOMINATOR-BLACKBOX`, `R14-RAILWAY-NO-SECRET-STARTUP`,
  `R14-CAPABILITY-MATRIX`, and `R14-COMPAT-GATEWAY-ISOLATION`.
- Fail evidence: any protected REST or MCP operation succeeds without the
  configured secret or required test setup silently disappears.
- Containment/backtrack: isolate ingress, terminate fixture sessions, preserve
  redacted access logs, and leave credential rotation to authorized operations.

## R-15 - External Processor Boundary

- Owner: Privacy Owner.
- Reviewers: Security Architect, Provider Integration Owner, Test Architect.
- Timebox: 3 days.
- Dependencies: `G-ICM-01`, R-13, processor inventory and recording sinks.
- Hypothesis: strict/local projects produce zero outbound raw-content calls
  across compression, embeddings, hybrid/vision query embedding, migration,
  and fallback; permitted projects call only explicitly declared processors.
- Pass evidence: zero recording-sink calls for strict/local fixtures; every
  permitted attempt records project, provider, purpose, policy decision, data
  class, and source provenance without raw content in the receipt.
  Production provider-fallback composition, configurable hook delivery, and
  mesh requests are part of the denominator. Missing policy context, DNS
  failure, private/local resolution, unavailable policy, undeclared remote
  transport, or an implicit session without a bound project policy fails
  closed before raw content reaches a transport. Named cases:
  `R15-PROVIDER-ATTEMPT-DENOMINATOR`,
  `R15-FALLBACK-FACTORY-COMPOSITION`, `R15-HOOK-REMOTE-EGRESS`,
  `R15-MESH-SSRF-DNS`, and `R15-POLICY-UNAVAILABLE`.
- Fail evidence: prohibited egress, unrecorded fallback, provider substitution
  without policy evaluation, or raw content in an audit receipt.
- Containment/backtrack: block provider network access, force local processing,
  preserve request metadata only, and discard the isolated provider state.

## R-16 - Atomic Migration and Complete Restore

- Owner: State Migration and Recovery Owner.
- Reviewers: Software Architect, Test Architect, Configuration Manager.
- Timebox: 3 days.
- Dependencies: `G-ICM-01`, R-01, R-13, complete persisted-state inventory.
- Hypothesis: the packaged migration command exists and every injected failure
  leaves either the exact pre-generation or exact target-generation state;
  restore reproduces the declared state manifest without additive residue.
- Pass evidence: emitted command target, per-namespace counts/hashes, failure
  ledger for every write boundary, idempotent resume, exact post-restore
  equality, rebuilt indexes, and preserved audit truth.
  Concurrent readers observe only the pre-operation or target immutable
  generation; process death at every stage/journal/index/activation boundary
  cannot expose mixed state. Named cases:
  `R16-NAMESPACE-MANIFEST-COMPLETENESS`,
  `R16-ACTIVE-GENERATION`, `R16-SNAPSHOT-RESTORE-GENERATION`, and
  `R16-MIGRATION-CRASH-RECOVERY`.
- Fail evidence: missing command target, mixed generation, omitted scope,
  orphan/duplicate record, post-snapshot residue, or unrecoverable interruption.
- Containment/backtrack: immutable pre-image and disposable copies only; stop
  before touching live state or retiring any Codebase Memory alias.

## R-17 - Typed Context Degradation

- Owner: Context Pipeline Owner.
- Reviewers: Product Owner, Software Architect, Security Architect, Test Architect.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-13, required/optional source contract.
- Hypothesis: every required context-source failure returns non-success, while
  an allowed optional failure returns typed `degraded` with source errors and
  completeness metadata.
- Pass evidence: single and combined faults for slots, profile, lessons,
  episodic search, and file history produce the declared state; no failed
  source is marked injected and no promotion can use the incomplete packet.
  The source-state matrix distinguishes `READY`, `EMPTY_VALID`, `DISABLED`,
  `NOT_REQUESTED`, `TIMEOUT`, and `ERROR`; valid-empty data cannot be
  reclassified as a dependency outage. Hook callers receive success only after
  the governed capture reaches its declared durable terminal state; a timeout,
  drop, or swallowed exception is explicit. Named case:
  `R17-CONTEXT-SOURCE-FAULT-MATRIX`.
- Fail evidence: unqualified `success:true`, silent empty substitution,
  missing source diagnostics, premature injection marker, or promotion from a
  degraded packet.
- Containment/backtrack: disable automatic context injection, use explicit
  advisory recall only, reset fixture markers, and invalidate affected packets.

## R-18 - MCP Authority Downgrade

- Owner: MCP Compatibility Owner.
- Reviewers: Security Architect, Authentication Service Owner, Test Architect,
  Configuration Manager.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-13, accepted compatibility/offline-mode
  contract, protected-interface and governed-side-effect manifests.
- Hypothesis: once a client selects authenticated proxy mode, no
  authentication, authorization, project-binding, required-backend, or
  protected-operation failure can execute through a less authoritative local
  path. Any separately accepted offline mode is explicit before invocation,
  project-scoped, visibly degraded, bound to a distinct local-state
  provenance, and limited to a human-accepted advisory operation allowlist.
- Pass evidence: a frozen status/error matrix covers connection refusal,
  timeout, malformed response, 401, 403, 404, 409, 422, 429, and 5xx for all
  seven locally implemented standalone tools and every server-only tool.
  Authentication/authorization/project failures and every global, delete,
  export, audit, migration, promotion, or gate-critical operation produce no
  fallback read, write, persistence, suppression, promotion, or fabricated
  success. If an offline advisory mode is accepted, its positive cases prove
  explicit activation, exact project scope, operation allowlist, separate
  namespace/provenance, visible degraded status, and zero access to server
  state. Named cases: `R18-PROXY-ERROR-CLASSIFICATION`,
  `R18-AUTH-FAIL-NO-FALLBACK`, `R18-GLOBAL-LOCAL-DENIAL`,
  `R18-MUTATION-SIDE-EFFECT-DENOMINATOR`, and
  `R18-EXPLICIT-OFFLINE-ADVISORY-MODE`.
- Fail evidence: any protected proxy failure returns local success, any local
  global read lacks administrator authority, any project-plus-global request
  is accepted, or any fallback mutation occurs outside an explicitly accepted
  and attributable mode.
- Containment/backtrack: disable implicit local fallback for protected tools,
  preserve current standalone stores as read-only evidence, isolate synthetic
  homes, and restore the pre-run executable/configuration without touching
  production memory.

## R-19 - Explicit Project-Scoped Native Memory Synchronization

- Owner: Native Memory Integration Owner.
- Reviewers: Security Architect, Privacy Owner, Test Architect, Product Owner.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-01, R-02, R-13, R-15, accepted explicit-action
  and exact project-selection contract.
- Hypothesis: a provider-native memory write occurs only after an explicit
  user action bound to one verified canonical project, and the synchronized
  source set, destination, diagnostics, and rollback contain no other
  project's content.
- Pass evidence: two-project canary fixtures prove zero cross-project source
  selection and destination disclosure across session-end, pre-compaction,
  direct API, error, retry, and backup paths; every write has a matching
  user-action receipt and exact project/destination identity.
- Fail evidence: any automatic write, project-blind list, cross-project canary,
  ambiguous destination, or provider-file mutation without matching explicit
  authority.
- Containment/backtrack: disable every automatic bridge trigger, block the
  recording destination, restore the fixture provider-file pre-image, and
  preserve only redacted immutable receipts.
- Admission state: card version 1 is `SPECIFICATION-CANDIDATE`; execution is not
  admitted.

## R-20 - Session Lifecycle Identity Integrity

- Owner: Session Lifecycle Owner.
- Reviewers: Security Architect, Configuration Manager, Test Architect,
  Software Architect.
- Timebox: 3 days.
- Dependencies: `G-ICM-01`, R-01, R-06, R-13, accepted session authority,
  parent-binding, and compare-and-swap lifecycle contract.
- Hypothesis: only an authorized caller bound to the canonical project and
  worktree can create, resume, parent, mutate, or close a session, and every
  concurrent or stale operation yields one linearizable lifecycle state with
  consistent counters and attribution.
- Pass evidence: spoofed identity, cross-project parent, stale token, resumed
  session, simultaneous close/update, and process-restart fixtures are denied
  or converge to the frozen state-machine oracle with no partial child/parent
  links and no counter drift.
- Fail evidence: unauthorized lifecycle mutation, stale closure winning over a
  current owner, cross-project linkage, mixed attribution, partial state, or
  counter inconsistency.
- Containment/backtrack: stop lifecycle mutations, quarantine fixture
  namespaces, restore the immutable pre-image, and invalidate fixture
  authority tokens.
- Admission state: card version 1 is `SPECIFICATION-CANDIDATE`; execution is not
  admitted.

## R-21 - Durable Exact-Event Capture Identity

- Owner: Capture Integrity Owner.
- Reviewers: Test Architect, Security Architect, Data Governance Owner.
- Timebox: 2 days.
- Dependencies: `G-ICM-01`, R-07, R-13, accepted exact-event identity,
  durability, and terminal receipt contract.
- Hypothesis: distinct full capture events remain distinct, while exact retries
  across concurrency and restart produce one durable terminal result whose
  receipt cannot report success before all governed side effects complete.
- Pass evidence: common-prefix/different-suffix, simultaneous duplicate,
  restart, and write/index/count/stream fault fixtures match the immutable
  event oracle with no lost corroboration, duplicate terminal state, or
  fabricated retry success.
- Fail evidence: prefix collision, check/record race, process-restart replay,
  distinct evidence collapse, duplicate side effect, or success receipt
  preceding a terminal durable state.
- Containment/backtrack: halt fixture ingestion, retain the append-only input
  oracle, discard the isolated derived state, and replay only after receipt
  reconciliation.
- Admission state: card version 1 is `SPECIFICATION-CANDIDATE`; execution is not
  admitted.

## R-22 - Atomic Compaction Generation and Ledger Integrity

- Owner: State Compaction Owner.
- Reviewers: Data Governance Owner, Security Architect, Test Architect,
  Configuration Manager.
- Timebox: 3 days.
- Dependencies: `G-ICM-01`, R-13, R-16, accepted compaction generation,
  exact-fact integrity, reader, and recovery contracts.
- Hypothesis: compaction publishes one integrity-bound generation atomically,
  so concurrent readers and recovery observe either the complete pre-image or
  complete target image and any exact-facts ledger alteration is detected.
- Pass evidence: faults at every ledger/write/delete/index/activation boundary,
  concurrent readers, restart, and ledger mutation fixtures expose no mixed
  generation, lost exact fact, orphan index, or undetected alteration; restart
  deterministically commits or rolls back.
- Fail evidence: mixed visibility, deleted source without valid target,
  unindexed target, orphan index, replay divergence, or undetected ledger
  tampering.
- Containment/backtrack: freeze compaction, retain immutable pre-images and
  journal receipts, discard the fixture target generation, and restore the
  verified active-generation pointer.
- Admission state: card version 1 is `SPECIFICATION-CANDIDATE`; execution is not
  admitted.

## R-23 - Worker Replay and Startup Reconciliation

- Owner: Runtime Supervision Owner.
- Reviewers: Operations Owner, Test Architect, Configuration Manager,
  Security Architect.
- Timebox: 3 days.
- Dependencies: `G-ICM-01`, R-07, R-08, R-13, R-21, accepted durable intake,
  singleton supervision, replay, and readiness contracts.
- Hypothesis: once hook intake returns durable acceptance, worker crash,
  forced termination, concurrent supervisor start, or host restart preserves
  exactly-once governed outcomes through bounded replay and startup
  reconciliation, with readiness remaining non-healthy until convergence.
- Pass evidence: crash points before and after each governed side effect,
  restart loops, stale PID, and dual-start fixtures reconcile the durable input
  oracle to one terminal outcome per event with no loss, duplicate, fabricated
  success, or premature readiness.
- Fail evidence: accepted event loss, duplicate side effect, multiple active
  workers, stale PID takeover, unreconciled partial event, unbounded replay, or
  healthy status before reconciliation.
- Containment/backtrack: close intake, terminate fixture workers, preserve the
  durable oracle and process receipts, restore the supervisor pre-image, and
  replay only from the last verified checkpoint.
- Admission state: card version 1 is `SPECIFICATION-CANDIDATE`; execution is not
  admitted.

## Dependency-Aware Retirement Evidence Sequence

1. Freeze the evidence revision, criteria, receipt schema, synthetic corpora,
   and `G-ICM-01`.
2. Obtain Stage-A specification acceptance, separately authorize B1 disposable
   mechanics, freeze them with the qualification inputs, separately admit the
   exact R-13 v3 bundle at B2, then establish independently reviewed Stage-D
   R-13 evidence so every later receipt has a deterministic denominator and
   portable authority.
3. After separate admission of each P1 card, collect R-02, R-14, R-18, and
   R-19 evidence in isolated sinks. Admit the P2 R-15 targeted method
   separately under the P2 hard-veto specification; it is not a `build-poc`
   card.
4. After separate admission, run the P2 R-01 targeted identity method, then
   the P1 R-20, R-16, R-22, and R-06 cards to establish identity, lifecycle,
   recoverability, compaction, and provenance foundations.
5. After separate admission, collect P1 R-17, R-04, R-03, and R-21 evidence.
   Admit the P2 R-05 targeted promotion-lineage method separately.
6. After separate admission, collect P1 R-07, R-23, and R-09 evidence. Admit
   the P2 R-08 targeted health/readiness method separately.
7. Rehearse P3 R-11 through its governed targeted method and admit the P1 R-10
   card separately for sandbox-only alias evidence.
8. Rehearse P2 R-12 offline through its separately admitted targeted method
   only after all predecessor evidence exists. It is not a `build-poc` card.
9. Have each accountable owner and independent reviewers assess raw receipts.
   Only a later gate cycle may change risk dispositions.
